{{/* External Secrets removed: no passwords stored or managed.
     - Application authenticates via IAM (Cloud SQL Proxy --auto-iam-authn)
     - Migration job sets an ephemeral postgres password via Cloud SQL Admin API */}}
